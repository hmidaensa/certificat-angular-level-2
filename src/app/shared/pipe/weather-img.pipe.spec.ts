import { WeatherImgPipe } from './weather-img.pipe';

describe('WeatherImgPipe', () => {
  it('create an instance', () => {
    //const pipe = new WeatherImgPipe();
    //expect(pipe).toBeTruthy();
  });
});
